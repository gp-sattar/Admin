<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.grampower.admin">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        xmlns:tools="http://schemas.android.com/tools"
        tools:replace="android:icon">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AssignTask"  android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".employeeListing" />
        <activity android:name=".GetTaskReports" />
        <activity android:name=".CalenderDatePicker" />
        <activity android:name=".EmployeeAttendanceCheck" />
        <activity android:name=".DeleteNotification" />
        <activity android:name=".TodayAttendance"></activity>
    </application>

</manifest>